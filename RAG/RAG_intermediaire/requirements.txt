# --- Cœur LangChain (écosystème 0.3.x) ---
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0
langchain-chroma>=0.2.0
langchain-huggingface>=0.1.2
langchain-ollama>=0.2.0

# --- Vector store ---
chromadb>=0.5.0

# --- Embeddings & reranking (local, gratuit, sans clé) ---
sentence-transformers>=3.0.0

# --- Recherche lexicale BM25 ---
rank-bm25>=0.2.2

# --- Frontend ---
streamlit>=1.36.0

# --- Mode offline / CI (TF-IDF + lecteur extractif, sans téléchargement) ---
scikit-learn>=1.3.0

# --- Divers ---
pyyaml>=6.0
requests>=2.31.0

# --- Évaluation RAGAS (OPTIONNEL : décommenter si evaluation.run_ragas = true) ---
ragas>=0.2.0
datasets>=2.18.0
pandas>=2.0.0
